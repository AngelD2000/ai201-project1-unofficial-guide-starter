sentence-transformers==3.4.1
chromadb>=0.6.0
groq==0.15.0
python-dotenv==1.0.1

# Query interface — add whichever you use for Milestone 5:
# gradio>=6.9.0
# streamlit>=1.40.0

# Document ingestion — used by fetchers/fetch_reddit_from_pdf.py and
# fetchers/fetch_rmp_from_pdf.py to extract text from browser-printed PDFs.
pdfplumber==0.11.4
